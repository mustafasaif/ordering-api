const path = require("path");

module.exports = {
  config: path.resolve("./config/db.config.js"), // Replace with the actual path to your db.config.js
  "models-path": path.resolve("./models"), // Replace with the actual path to your models directory
  "seeders-path": path.resolve("./seeders"), // Replace with the actual path to your seeders directory
  "migrations-path": path.resolve("./migrations"), // Replace with the actual path to your migrations directory
};
